import { keyLesson } from './helpers.js';

export const modeLessons = [
  keyLesson({
    id: 'modes.insert',
    topic: 'modes',
    keys: 'i',
    label: 'Enter Insert mode',
    prompt: 'Start inserting text before the cursor.',
    explains: 'The statusline changes from NORMAL to INSERT; Escape returns to Normal mode.',
    siblings: ['a', 'v', ':'],
    sim: 'buffer',
  }),
  keyLesson({
    id: 'modes.append',
    topic: 'modes',
    keys: 'a',
    label: 'Append after cursor',
    prompt: 'Enter Insert mode after the current character.',
    explains: 'Unlike i, append moves the insertion point one character to the right.',
    siblings: ['i', 'v'],
    sim: 'buffer',
  }),
  keyLesson({
    id: 'modes.visual',
    topic: 'modes',
    keys: 'v',
    label: 'Enter Visual mode',
    prompt: 'Begin a character-wise visual selection.',
    explains: 'Motions extend the selection until Escape returns to Normal mode.',
    siblings: ['V', 'i', '<Esc>'],
    sim: 'buffer',
  }),
  keyLesson({
    id: 'modes.visual_line',
    topic: 'modes',
    keys: 'V',
    label: 'Enter Visual Line mode',
    prompt: 'Select the entire current line.',
    explains: 'Each motion extends the selection by complete lines.',
    siblings: ['v', '<Esc>'],
    sim: 'buffer',
  }),
  keyLesson({
    id: 'modes.command',
    topic: 'modes',
    keys: ':',
    label: 'Enter Command-line mode',
    prompt: 'Open the Ex command prompt.',
    explains: 'Type commands such as :write, :quit, :Mason, or :SF.',
    siblings: ['i', 'v', '<Esc>'],
    sim: 'command',
  }),
  keyLesson({
    id: 'modes.normal',
    topic: 'modes',
    keys: '<Esc>',
    mode: 'INSERT/VISUAL/COMMAND',
    label: 'Return to Normal mode',
    prompt: 'Leave the current editing/selection/command mode.',
    explains: 'Escape returns control to Normal mode. In terminal mode this config uses double Escape.',
    siblings: ['<Esc><Esc>', 'i', 'v'],
    sim: 'buffer',
  }),
];
