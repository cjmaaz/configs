import { keyLesson } from './helpers.js';

const topic = 'editing';

export const editingLessons = [
  keyLesson({
    id: 'editing.format',
    topic,
    keys: '<leader>f',
    label: 'Format buffer',
    prompt: 'Format the current buffer or Visual selection.',
    explains: 'Conform picks the configured formatter and falls back to LSP formatting.',
    siblings: ['<leader>tf', '<leader>cl', '<leader>fe'],
    sim: 'format',
  }),
  keyLesson({
    id: 'editing.toggle_format',
    topic,
    keys: '<leader>tf',
    label: 'Toggle format-on-save',
    prompt: 'Enable or disable automatic formatting when saving.',
    explains: 'Toggles vim.g.disable_autoformat and notifies the new state.',
    siblings: ['<leader>f', '<leader>cl'],
    sim: 'format',
  }),
  keyLesson({
    id: 'editing.lint',
    topic,
    keys: '<leader>cl',
    label: 'Lint buffer',
    prompt: 'Run the configured linter for the current file.',
    explains: 'Calls nvim-lint manually; linting also runs on buffer enter, write, and InsertLeave.',
    siblings: ['<leader>ca', '<leader>f'],
    sim: 'lsp-hover',
  }),
  keyLesson({
    id: 'editing.todo_next',
    topic,
    keys: ']t',
    label: 'Next TODO',
    prompt: 'Jump to the next TODO/FIXME comment.',
    explains: 'todo-comments navigates recognized annotation keywords.',
    siblings: ['[t', '<leader>st'],
    sim: 'buffer',
  }),
  keyLesson({
    id: 'editing.todo_previous',
    topic,
    keys: '[t',
    label: 'Previous TODO',
    prompt: 'Jump to the previous TODO/FIXME comment.',
    explains: 'Moves backward through todo-comments annotations.',
    siblings: [']t', '<leader>st'],
    sim: 'buffer',
  }),
  keyLesson({
    id: 'editing.todo_search',
    topic,
    keys: '<leader>st',
    label: 'Search TODOs',
    prompt: 'Show project TODO/FIXME comments in Telescope.',
    explains: 'Runs :TodoTelescope for project-wide annotation search.',
    siblings: [']t', '[t', '<leader>sg'],
    sim: 'telescope-picker',
  }),
  keyLesson({
    id: 'editing.mini_around_next',
    topic,
    keys: 'aa',
    mode: 'OPERATOR/VISUAL',
    label: 'Next around text object',
    prompt: 'Select the next “around” mini.ai text object.',
    explains: 'aa avoids Neovim 0.12’s built-in an incremental-selection mapping.',
    siblings: ['ii', 'ih'],
    sim: 'buffer',
  }),
  keyLesson({
    id: 'editing.mini_inside_next',
    topic,
    keys: 'ii',
    mode: 'OPERATOR/VISUAL',
    label: 'Next inside text object',
    prompt: 'Select the next “inside” mini.ai text object.',
    explains: 'ii is the inside counterpart to aa.',
    siblings: ['aa', 'ih'],
    sim: 'buffer',
  }),
  keyLesson({
    id: 'editing.surround_add',
    topic,
    keys: 'saiw)',
    label: 'Add surrounding parentheses',
    prompt: 'Surround the inner word with parentheses.',
    explains: 'mini.surround reads sa + text object iw + destination ).',
    siblings: ["sd'", "sr)'"],
    sim: 'buffer',
  }),
  keyLesson({
    id: 'editing.surround_delete',
    topic,
    keys: "sd'",
    label: 'Delete surrounding quotes',
    prompt: 'Delete the nearest single-quote surrounding pair.',
    explains: 'mini.surround uses sd followed by the surrounding character.',
    siblings: ['saiw)', "sr)'"],
    sim: 'buffer',
  }),
  keyLesson({
    id: 'editing.surround_replace',
    topic,
    keys: "sr)'",
    label: 'Replace surroundings',
    prompt: 'Replace surrounding parentheses with single quotes.',
    explains: 'mini.surround reads sr, the old surrounding ), then the new one \'.',
    siblings: ['saiw)', "sd'"],
    sim: 'buffer',
  }),
  keyLesson({
    id: 'editing.complete_next',
    topic,
    keys: '<Tab>',
    mode: 'INSERT',
    label: 'Next completion/snippet stop',
    prompt: 'Select the next completion item or jump forward in a snippet.',
    explains: 'blink.cmp tries select_next, then snippet_forward, then falls back.',
    siblings: ['<S-Tab>', '<CR>'],
    sim: 'completion',
  }),
  keyLesson({
    id: 'editing.complete_previous',
    topic,
    keys: '<S-Tab>',
    mode: 'INSERT',
    label: 'Previous completion/snippet stop',
    prompt: 'Select the previous completion item or jump backward in a snippet.',
    explains: 'blink.cmp tries select_prev, then snippet_backward, then falls back.',
    siblings: ['<Tab>', '<CR>'],
    sim: 'completion',
  }),
  keyLesson({
    id: 'editing.complete_accept',
    topic,
    keys: '<CR>',
    mode: 'INSERT',
    label: 'Accept completion',
    prompt: 'Accept the selected completion item.',
    explains: 'blink.cmp accepts the selected candidate; otherwise Enter keeps its normal behavior.',
    siblings: ['<Tab>', '<S-Tab>'],
    sim: 'completion',
  }),
];
