import { settingLesson } from './helpers.js';

const topic = 'options';
const lesson = (config) => settingLesson({ topic, sim: 'settings', ...config });
const booleanChoices = (enabledEffect, disabledEffect) => [
  { value: true, label: 'true', effect: enabledEffect },
  { value: false, label: 'false', effect: disabledEffect },
];

export const optionLessons = [
  lesson({
    id: 'options.number',
    setting: 'number',
    value: true,
    label: 'Absolute line number',
    prompt: 'Is the current line shown with its absolute line number?',
    explains: 'number and relativenumber work together: current line is absolute, surrounding lines are relative.',
    choices: booleanChoices('Shows the current absolute line number.', 'Hides absolute line numbers.'),
  }),
  lesson({
    id: 'options.relativenumber',
    setting: 'relativenumber',
    value: true,
    label: 'Relative line numbers',
    prompt: 'Which value makes surrounding line numbers show motion distance?',
    explains: 'Relative numbers make commands such as 8j and 5k easy to estimate.',
    choices: booleanChoices('Shows distance from the cursor.', 'Shows only absolute line numbers.'),
  }),
  lesson({
    id: 'options.mouse',
    setting: 'mouse',
    value: 'a',
    label: 'Mouse in all modes',
    prompt: 'Which mouse value enables selection and split resizing in every mode?',
    explains: 'The value “a” means all supported modes.',
    choices: [
      { value: '', label: 'Disabled', effect: 'Disables mouse input.' },
      { value: 'n', label: 'Normal only', effect: 'Enables mouse input in Normal mode.' },
      { value: 'a', label: 'All modes', effect: 'Enables mouse support everywhere.' },
    ],
  }),
  lesson({
    id: 'options.showmode',
    setting: 'showmode',
    value: false,
    label: 'Built-in mode message',
    prompt: 'Is Neovim’s built-in -- INSERT -- mode message enabled?',
    explains: 'It is disabled because the simulated lualine statusline owns the mode indicator.',
    choices: booleanChoices('Duplicates the statusline mode text.', 'Leaves mode display to lualine.').reverse(),
  }),
  lesson({
    id: 'options.clipboard',
    setting: 'clipboard',
    value: 'unnamedplus',
    label: 'System clipboard',
    prompt: 'Which clipboard value synchronizes yank/paste with the OS clipboard?',
    explains: 'It is scheduled after UI startup so clipboard-provider detection does not slow startup.',
    choices: [
      { value: '', label: 'Private registers', effect: 'Keeps Neovim and OS clipboards separate.' },
      { value: 'unnamed', label: 'Primary selection', effect: 'Uses the * register where supported.' },
      { value: 'unnamedplus', label: 'System clipboard', effect: 'Uses the + register for OS copy/paste.' },
    ],
  }),
  lesson({
    id: 'options.breakindent',
    setting: 'breakindent',
    value: true,
    label: 'Wrapped-line indentation',
    prompt: 'Should wrapped text preserve the indentation of its source line?',
    explains: 'breakindent makes long wrapped lines easier to follow.',
    choices: booleanChoices('Wrapped continuations stay indented.', 'Wrapped text starts at column zero.'),
  }),
  lesson({
    id: 'options.undofile',
    setting: 'undofile',
    value: true,
    label: 'Persistent undo',
    prompt: 'Should undo history survive closing and reopening a file?',
    explains: 'Neovim writes an undo file under its state directory.',
    choices: booleanChoices('Undo history persists across sessions.', 'Undo history ends when the buffer closes.'),
  }),
  lesson({
    id: 'options.search_case',
    setting: 'ignorecase + smartcase',
    value: 'smart',
    label: 'Smart-case search',
    prompt: 'How does this config decide whether search is case-sensitive?',
    explains: 'ignorecase enables forgiving searches; smartcase restores sensitivity when the query contains capitals.',
    choices: [
      { value: 'always-sensitive', label: 'Always sensitive', effect: 'Every character must match case.' },
      { value: 'always-insensitive', label: 'Always insensitive', effect: 'Capital letters never affect matching.' },
      { value: 'smart', label: 'Smart case', effect: 'Lowercase is insensitive; capitals opt into sensitivity.' },
    ],
  }),
  lesson({
    id: 'options.signcolumn',
    setting: 'signcolumn',
    value: 'yes',
    label: 'Persistent sign column',
    prompt: 'Which value prevents text shifting when diagnostics or git signs appear?',
    explains: 'Always reserving the sign column keeps code horizontally stable.',
    choices: [
      { value: 'auto', label: 'auto', effect: 'Shows the column only when signs exist, shifting text.' },
      { value: 'yes', label: 'yes', effect: 'Always reserves one sign column.' },
      { value: 'no', label: 'no', effect: 'Hides all signs.' },
    ],
  }),
  lesson({
    id: 'options.updatetime',
    setting: 'updatetime',
    value: 250,
    label: 'Update delay',
    prompt: 'How many milliseconds pass before CursorHold-style events can fire?',
    explains: '250 ms makes diagnostics, gitsigns, and hover-related updates feel responsive.',
    choices: [
      { value: 100, label: '100 ms', effect: 'Very responsive but triggers background work more often.' },
      { value: 250, label: '250 ms', effect: 'Responsive without excessive event churn.' },
      { value: 4000, label: '4000 ms', effect: 'Neovim’s historically slow default.' },
    ],
  }),
  lesson({
    id: 'options.timeoutlen',
    setting: 'timeoutlen',
    value: 300,
    label: 'Chord timeout',
    prompt: 'How long does Neovim wait for the next key in a mapped sequence?',
    explains: 'The game uses the same 300 ms chord window.',
    choices: [
      { value: 100, label: '100 ms', effect: 'Fast but unforgiving for multi-key leaders.' },
      { value: 300, label: '300 ms', effect: 'Quick and still learnable.' },
      { value: 1000, label: '1000 ms', effect: 'Beginner-friendly but makes partial mappings feel laggy.' },
    ],
  }),
  lesson({
    id: 'options.splits',
    setting: 'splitright + splitbelow',
    value: 'right-below',
    label: 'Predictable split direction',
    prompt: 'Where do new vertical and horizontal splits open?',
    explains: 'Both options match common reading direction and the window-navigation maps.',
    choices: [
      { value: 'left-above', label: 'Left and above', effect: 'Uses legacy Vim placement.' },
      { value: 'right-below', label: 'Right and below', effect: 'Places new work in reading order.' },
      { value: 'right-above', label: 'Right and above', effect: 'Uses mixed placement.' },
    ],
  }),
  lesson({
    id: 'options.scrolloff',
    setting: 'scrolloff',
    value: 8,
    label: 'Vertical context',
    prompt: 'How many context lines stay visible above and below the cursor?',
    explains: 'Eight balances context with usable screen space.',
    choices: [
      { value: 0, label: '0', effect: 'Allows the cursor to touch the window edge.' },
      { value: 8, label: '8', effect: 'Keeps a comfortable context margin.' },
      { value: 10, label: '10', effect: 'Kickstart’s larger context margin with less visible editing space.' },
    ],
  }),
  lesson({
    id: 'options.sidescrolloff',
    setting: 'sidescrolloff',
    value: 8,
    label: 'Horizontal context',
    prompt: 'How many columns stay visible beside the cursor when horizontally scrolling?',
    explains: 'This mirrors the vertical context margin.',
    choices: [
      { value: 0, label: '0', effect: 'Cursor may touch the horizontal edge.' },
      { value: 8, label: '8', effect: 'Keeps eight columns of context.' },
      { value: 20, label: '20', effect: 'Keeps more context but shows less long-line content.' },
    ],
  }),
  lesson({
    id: 'options.cursorline',
    setting: 'cursorline',
    value: true,
    label: 'Current-line highlight',
    prompt: 'Is the current line visually highlighted?',
    explains: 'The simulator mirrors this with a darker current-line background.',
    choices: booleanChoices('Highlights the entire current line.', 'Uses only the cursor position.'),
  }),
  lesson({
    id: 'options.confirm',
    setting: 'confirm',
    value: true,
    label: 'Unsaved-change confirmation',
    prompt: 'What happens when a command would discard unsaved changes?',
    explains: 'confirm opens a save/discard/cancel dialog instead of failing immediately.',
    choices: booleanChoices('Prompts with a confirmation dialog.', 'Returns an error and requires a force command.'),
  }),
  lesson({
    id: 'options.indentation',
    setting: 'expandtab + shiftwidth',
    value: 'spaces-2',
    label: 'Two-space indentation',
    prompt: 'What does pressing Tab insert in this config?',
    explains: 'expandtab with tabstop/softtabstop/shiftwidth 2 creates consistent two-space indentation.',
    choices: [
      { value: 'tabs-8', label: 'A literal 8-column tab', effect: 'Classic Vim defaults.' },
      { value: 'spaces-2', label: 'Two spaces', effect: 'Current web/Lua-friendly behavior.' },
      { value: 'spaces-4', label: 'Four spaces', effect: 'Common Python/Java style.' },
    ],
  }),
  lesson({
    id: 'options.wrap',
    setting: 'wrap + linebreak',
    value: 'nowrap',
    label: 'Long-line display',
    prompt: 'Do long code lines wrap onto additional screen rows?',
    explains: 'wrap is false. linebreak is ready if wrapping is enabled later.',
    choices: [
      { value: 'nowrap', label: 'No wrapping', effect: 'Long lines scroll horizontally.' },
      { value: 'hard-wrap', label: 'Hard wrap', effect: 'Physically inserts line breaks.' },
      { value: 'soft-wrap', label: 'Soft wrap', effect: 'Displays wrapped rows without changing the file.' },
    ],
  }),
  lesson({
    id: 'options.listchars',
    setting: 'list + listchars',
    value: 'visible',
    label: 'Visible whitespace',
    prompt: 'How are tabs, trailing spaces, and non-breaking spaces displayed?',
    explains: 'Tabs use » , trailing spaces use ·, and non-breaking spaces use ␣.',
    choices: [
      { value: 'hidden', label: 'Hidden', effect: 'Whitespace looks blank.' },
      { value: 'visible', label: 'Visible glyphs', effect: 'Makes invisible formatting problems obvious.' },
      { value: 'all-dots', label: 'Every space as dot', effect: 'Very explicit but visually noisy.' },
    ],
  }),
  lesson({
    id: 'options.inccommand',
    setting: 'inccommand',
    value: 'split',
    label: 'Live substitution preview',
    prompt: 'Where are :substitute changes previewed while typing?',
    explains: 'The split value opens a preview window with the prospective edits.',
    choices: [
      { value: '', label: 'Disabled', effect: 'Shows changes only after executing.' },
      { value: 'nosplit', label: 'Inline', effect: 'Previews directly in the buffer.' },
      { value: 'split', label: 'Preview split', effect: 'Shows affected lines in a dedicated split.' },
    ],
  }),
  lesson({
    id: 'options.completeopt',
    setting: 'completeopt',
    value: 'menu,menuone,noselect',
    label: 'Completion menu behavior',
    prompt: 'Does completion automatically select the first candidate?',
    explains: 'noselect keeps acceptance explicit; menuone shows the menu even for one item.',
    choices: [
      { value: 'menu,menuone,noselect', label: 'Explicit selection', effect: 'Nothing is accepted until you choose it.' },
      { value: 'menu,menuone', label: 'Auto-select', effect: 'The first candidate may be preselected.' },
      { value: 'longest', label: 'Insert common prefix', effect: 'Immediately inserts shared candidate text.' },
    ],
  }),
  lesson({
    id: 'options.pumheight',
    setting: 'pumheight',
    value: 12,
    label: 'Completion menu height',
    prompt: 'How many completion entries can the popup show at once?',
    explains: 'Twelve keeps suggestions useful without covering the editor.',
    choices: [
      { value: 5, label: '5', effect: 'Compact but requires more scrolling.' },
      { value: 12, label: '12', effect: 'Current balanced height.' },
      { value: 0, label: 'Unlimited', effect: 'Can cover most of the editor.' },
    ],
  }),
  lesson({
    id: 'options.smoothscroll',
    setting: 'smoothscroll',
    value: true,
    label: 'Smooth display-line scrolling',
    prompt: 'Does scrolling operate smoothly through wrapped display lines?',
    explains: 'Enabled on supported Neovim versions.',
    choices: booleanChoices('Scrolls by screen lines through wrapped text.', 'Uses classic logical-line scrolling.'),
  }),
];
