import { keyLesson, settingLesson } from './helpers.js';

const topic = 'salesforce';
const lesson = (config) => keyLesson({ topic, sim: 'sf-terminal', ...config });
const setting = (config) => settingLesson({ topic, sim: 'settings', ...config });

export const salesforceLessons = [
  lesson({
    id: 'sf.fetch_orgs',
    keys: '<leader>SF',
    label: 'Fetch orgs',
    prompt: 'Refresh the list of authenticated Salesforce orgs.',
    explains: 'Runs sf org list through sf.nvim and caches available aliases/target org.',
    siblings: ['<leader>So', '<leader>SO', '<leader>Sb'],
  }),
  lesson({
    id: 'sf.set_org',
    keys: '<leader>So',
    label: 'Set project target org',
    prompt: 'Choose the target org for this Salesforce project.',
    explains: 'Runs sf config set target-org without --global.',
    siblings: ['<leader>SO', '<leader>SF'],
  }),
  lesson({
    id: 'sf.set_global_org',
    keys: '<leader>SO',
    label: 'Set global target org',
    prompt: 'Choose the Salesforce target org globally.',
    explains: 'Runs sf config set target-org --global, affecting other workspaces too.',
    siblings: ['<leader>So', '<leader>SF'],
  }),
  lesson({
    id: 'sf.open_org',
    keys: '<leader>Sb',
    label: 'Open target org',
    prompt: 'Open the target Salesforce org in a browser.',
    explains: 'Delegates to sf org open for the selected alias.',
    siblings: ['<leader>SB', '<leader>So'],
  }),
  lesson({
    id: 'sf.open_metadata',
    keys: '<leader>SB',
    label: 'Open current metadata',
    prompt: 'Open the current metadata component in Salesforce Setup.',
    explains: 'Uses sf.nvim’s component-aware org URL for the current file.',
    siblings: ['<leader>Sb', '<leader>Sr', '<leader>Sd'],
  }),
  lesson({
    id: 'sf.retrieve',
    keys: '<leader>Sr',
    label: 'Retrieve current file',
    prompt: 'Retrieve the current metadata component from the target org.',
    explains: 'Runs sf project retrieve for the metadata represented by this buffer.',
    siblings: ['<leader>Sd', '<leader>SB'],
  }),
  lesson({
    id: 'sf.diff',
    keys: '<leader>Sd',
    label: 'Diff against target org',
    prompt: 'Compare the local metadata file with the target-org version.',
    explains: 'Retrieves a temporary remote copy and opens Neovim diff mode.',
    siblings: ['<leader>Sr', '<leader>SB'],
    sim: 'gitsigns',
  }),
  lesson({
    id: 'sf.pull_log',
    keys: '<leader>Sl',
    label: 'Pull debug log',
    prompt: 'Choose and download a Salesforce debug log.',
    explains: 'Lists available Apex logs and opens the selected log with sflog highlighting.',
    siblings: ['<leader>Se', '<leader>Sx'],
  }),
  lesson({
    id: 'sf.toggle_terminal',
    keys: '<leader>Se',
    label: 'Toggle Salesforce terminal',
    prompt: 'Show or hide sf.nvim’s integrated terminal.',
    explains: 'The integrated terminal is read-only, disposable, and displays the latest task.',
    siblings: ['<leader>Sx', '<Esc><Esc>'],
  }),
  lesson({
    id: 'sf.cancel',
    keys: '<leader>Sx',
    label: 'Cancel Salesforce command',
    prompt: 'Terminate the currently running sf.nvim command.',
    explains: 'Stops the active process in the integrated Salesforce terminal.',
    siblings: ['<leader>Se'],
  }),
  lesson({
    id: 'sf.test_current',
    keys: '<leader>St',
    label: 'Test under cursor',
    prompt: 'Run the Apex test method under the cursor.',
    explains: 'Treesitter identifies the @IsTest method; this fast variant omits coverage.',
    siblings: ['<leader>ST', '<leader>Sa', '<leader>SR'],
  }),
  lesson({
    id: 'sf.test_current_coverage',
    keys: '<leader>ST',
    label: 'Test under cursor with coverage',
    prompt: 'Run the Apex test under the cursor and collect coverage.',
    explains: 'Coverage results update signs and the lualine percentage.',
    siblings: ['<leader>St', '<leader>SA', '<leader>Sv'],
  }),
  lesson({
    id: 'sf.test_file',
    keys: '<leader>Sa',
    label: 'Test current file',
    prompt: 'Run every Apex test in the current test class.',
    explains: 'Runs all methods in this test file without coverage collection.',
    siblings: ['<leader>SA', '<leader>St', '<leader>SR'],
  }),
  lesson({
    id: 'sf.test_file_coverage',
    keys: '<leader>SA',
    label: 'Test file with coverage',
    prompt: 'Run every test in the current file and collect coverage.',
    explains: 'The slower variant updates covered/uncovered line signs.',
    siblings: ['<leader>Sa', '<leader>ST', '<leader>Sv'],
  }),
  lesson({
    id: 'sf.repeat_test',
    keys: '<leader>SR',
    label: 'Repeat last Apex test',
    prompt: 'Repeat the most recently executed Apex test command.',
    explains: 'Reuses the last test selection and coverage mode.',
    siblings: ['<leader>St', '<leader>Sa'],
  }),
  lesson({
    id: 'sf.toggle_coverage',
    keys: '<leader>Sv',
    label: 'Toggle coverage signs',
    prompt: 'Show or hide covered/uncovered Apex line signs.',
    explains: 'Uses the latest stored coverage result for the current Apex file.',
    siblings: ['[v', ']v', '<leader>ST'],
    sim: 'gitsigns',
  }),
  lesson({
    id: 'sf.previous_uncovered',
    keys: '[v',
    label: 'Previous uncovered line',
    prompt: 'Jump to the previous uncovered Apex line.',
    explains: 'Navigates backward through red coverage signs.',
    siblings: [']v', '<leader>Sv'],
    sim: 'gitsigns',
  }),
  lesson({
    id: 'sf.next_uncovered',
    keys: ']v',
    label: 'Next uncovered line',
    prompt: 'Jump to the next uncovered Apex line.',
    explains: 'Navigates forward through red coverage signs.',
    siblings: ['[v', '<leader>Sv'],
    sim: 'gitsigns',
  }),
  lesson({
    id: 'sf.run_soql',
    keys: '<leader>Sq',
    mode: 'VISUAL',
    label: 'Run selected SOQL',
    prompt: 'Execute the selected text as a SOQL query.',
    explains: 'Passes the Visual selection to sf data query in the integrated terminal.',
    siblings: ['<leader>SM', '<leader>Sl'],
  }),
  lesson({
    id: 'sf.pull_metadata',
    keys: '<leader>SM',
    label: 'Pull metadata inventory',
    prompt: 'Download the configured metadata-name inventories.',
    explains: 'This must run before the metadata picker has fresh names.',
    siblings: ['<leader>Sm', '<leader>SK'],
  }),
  lesson({
    id: 'sf.list_metadata',
    keys: '<leader>Sm',
    label: 'List metadata',
    prompt: 'Choose one metadata component to retrieve.',
    explains: 'Uses fzf-lua and the inventory created by <leader>SM.',
    siblings: ['<leader>SM', '<leader>Sk'],
    sim: 'telescope-picker',
  }),
  lesson({
    id: 'sf.pull_metadata_types',
    keys: '<leader>SK',
    label: 'Pull metadata types',
    prompt: 'Download the org’s metadata-type inventory.',
    explains: 'Prepares the whole-type picker used by <leader>Sk.',
    siblings: ['<leader>Sk', '<leader>SM'],
  }),
  lesson({
    id: 'sf.list_metadata_types',
    keys: '<leader>Sk',
    label: 'List metadata types',
    prompt: 'Choose a metadata type and retrieve all its components.',
    explains: 'Uses fzf-lua and the type inventory created by <leader>SK.',
    siblings: ['<leader>SK', '<leader>Sm'],
    sim: 'telescope-picker',
  }),
  lesson({
    id: 'sf.refresh_sobjects',
    keys: '<leader>Ss',
    label: 'Refresh SObject definitions',
    prompt: 'Refresh standard/custom SObject stubs for Apex completion.',
    explains: 'Writes faux Apex classes under .sfdx/tools/sobjects and restarts apex_ls.',
    siblings: ['<leader>SF', '<leader>Sc'],
  }),
  lesson({
    id: 'sf.create_ctags',
    keys: '<leader>Sc',
    label: 'Create Apex ctags',
    prompt: 'Generate a ctags file for fallback Apex navigation.',
    explains: 'Universal Ctags supplements apex_ls when LSP definition lookup misses.',
    siblings: ['gd', '<leader>Ss'],
  }),
  settingLesson({
    id: 'sf.hotkeys',
    topic,
    setting: 'enable_hotkeys',
    value: false,
    label: 'sf.nvim default hotkeys',
    prompt: 'Are sf.nvim’s broad built-in hotkeys enabled?',
    explains: 'They are disabled to avoid collisions; the uppercase <leader>S namespace replaces them.',
    choices: [
      { value: true, label: 'Enabled', effect: 'Installs all upstream buffer-local mappings and may overwrite yours.' },
      { value: false, label: 'Disabled', effect: 'Uses only this config’s explicit Salesforce mappings.' },
    ],
  }),
  settingLesson({
    id: 'sf.fetch_start',
    topic,
    setting: 'fetch_org_list_at_nvim_start',
    value: false,
    label: 'Manual org discovery',
    prompt: 'Does sf.nvim run sf org list automatically when it loads?',
    explains: 'Manual discovery avoids startup side effects; <leader>SF fetches when needed.',
    choices: [
      { value: true, label: 'Automatic', effect: 'Populates target org immediately but runs a CLI process on startup.' },
      { value: false, label: 'Manual', effect: 'Keeps startup fast and waits for <leader>SF.' },
    ],
  }),
  settingLesson({
    id: 'sf.terminal',
    topic,
    setting: 'terminal',
    value: 'integrated',
    label: 'Salesforce terminal strategy',
    prompt: 'Where do sf.nvim tasks run in this config?',
    explains: 'The integrated terminal avoids another dependency and focuses on the current task.',
    choices: [
      { value: 'integrated', label: 'Integrated terminal', effect: 'Disposable floating output, no task history.' },
      { value: 'overseer', label: 'Overseer', effect: 'Persistent task history/UI but requires overseer.nvim.' },
    ],
  }),
  settingLesson({
    id: 'sf.coverage',
    topic,
    setting: 'auto_display_code_sign',
    value: true,
    label: 'Automatic coverage signs',
    prompt: 'Are Apex coverage signs shown automatically after coverage tests?',
    explains: 'Covered/uncovered signs appear after tests and can be toggled with <leader>Sv.',
    choices: [
      { value: true, label: 'Automatic', effect: 'Shows signs immediately after a coverage run.' },
      { value: false, label: 'Manual', effect: 'Requires <leader>Sv before signs appear.' },
    ],
  }),

  setting({
    id: 'sf.log_narrowing',
    setting: 'log filetype override',
    value: 'paths',
    label: 'Narrowing the sflog filetype',
    prompt: 'Which .log files are treated as Salesforce debug logs?',
    explains:
      'Only those under /sf_cache/ or /.sfdx/. Left alone, sf.nvim claims every .log file, so an unrelated application log would open with Salesforce highlighting.',
    choices: [
      {
        value: 'paths',
        label: 'Only logs under sf_cache or .sfdx',
        effect: 'Other .log files stay a plain log filetype.',
      },
      { value: 'all', label: 'Every .log file', effect: 'The upstream sf.nvim default.' },
      { value: 'none', label: 'None', effect: 'Debug logs would lose their highlighting.' },
    ],
  }),
  setting({
    id: 'sf.no_deploy_key',
    setting: 'save_and_push',
    value: 'unbound',
    label: 'The deliberately missing deploy key',
    prompt: 'Why is there no keymap for sf.nvim save_and_push?',
    explains:
      'It deploys the current file to the org immediately, with no confirmation. Leaving it unbound prevents a mistyped chord from pushing to a shared org; the suggested binding is commented out in the spec.',
    choices: [
      {
        value: 'unbound',
        label: 'It deploys instantly, so it is left unbound on purpose',
        effect: 'A safety decision, documented in a comment.',
      },
      { value: 'missing', label: 'An oversight', effect: 'The commented suggestion shows it is deliberate.' },
      { value: 'unsupported', label: 'sf.nvim does not support it', effect: 'The function exists and works.' },
    ],
  }),
  setting({
    id: 'sf.types_to_retrieve',
    setting: 'types_to_retrieve',
    value: 'twelve',
    label: 'Retrievable metadata types',
    prompt: 'What limits which metadata types <leader>SM and <leader>Sm can list?',
    explains:
      'A twelve-entry list including ApexClass, ApexTrigger, LightningComponentBundle, Flow, PermissionSet and three Omni types. A type absent from the list will not appear in the picker.',
    choices: [
      {
        value: 'twelve',
        label: 'An explicit twelve-type list',
        effect: 'Includes OmniScript and related OmniStudio types.',
      },
      { value: 'all', label: 'Every type in the org', effect: 'The list is deliberately curated.' },
      { value: 'apex', label: 'Only Apex types', effect: 'Components and Flows are included too.' },
    ],
  }),
  setting({
    id: 'sf.fzf_split',
    setting: 'picker framework',
    value: 'fzf-lua',
    label: 'Two picker frameworks',
    prompt: 'Which picker do the sf.nvim menus use?',
    explains:
      'fzf-lua, pulled in solely as an sf.nvim dependency, while everything else in this config uses Telescope. sf.nvim integrates with fzf-lua directly, so the alternative would be no pickers at all.',
    choices: [
      {
        value: 'fzf-lua',
        label: 'fzf-lua, only for sf.nvim',
        effect: 'Telescope remains the general-purpose picker.',
      },
      { value: 'telescope', label: 'Telescope', effect: 'sf.nvim does not integrate with it.' },
      { value: 'both', label: 'Either, interchangeably', effect: 'sf.nvim supports only fzf-lua.' },
    ],
  }),
  setting({
    id: 'sf.hotkeys_filetypes',
    setting: 'hotkeys_in_filetypes',
    value: 'nine',
    label: 'Where sf hotkeys would apply',
    prompt: 'What is the point of hotkeys_in_filetypes when enable_hotkeys is false?',
    explains:
      'It scopes the built-in hotkeys to nine Salesforce-related filetypes if you ever enable them. With enable_hotkeys=false the list is inert, kept so switching it on stays safe.',
    choices: [
      {
        value: 'nine',
        label: 'It limits them to Salesforce filetypes if enabled',
        effect: 'Currently inert because hotkeys are off.',
      },
      { value: 'active', label: 'It enables them now', effect: 'enable_hotkeys is false.' },
      { value: 'nothing', label: 'It does nothing ever', effect: 'It matters the moment hotkeys are enabled.' },
    ],
  }),
  setting({
    id: 'sf.lazy_trigger',
    setting: 'sf.nvim lazy trigger',
    value: 'ft',
    label: 'When sf.nvim loads',
    prompt: 'What causes sf.nvim to load?',
    explains:
      'Opening one of nine Salesforce filetypes, or running :SF. Combined with fetch_org_list_at_nvim_start=false, a non-Salesforce session never pays for it.',
    choices: [
      {
        value: 'ft',
        label: 'A Salesforce filetype or the :SF command',
        effect: 'No cost in unrelated projects.',
      },
      { value: 'start', label: 'On startup', effect: 'Would slow every launch with a CLI call.' },
      { value: 'keys', label: 'On the first <leader>S chord', effect: 'The trigger is filetype and command based.' },
    ],
  }),
  setting({
    id: 'sf.action_indirection',
    setting: 'sf_action helper',
    value: 'lazy',
    label: 'Deferring the sf require',
    prompt: 'Why do the keymaps call a local sf_action helper instead of require("sf") directly?',
    explains:
      'The helper requires sf.nvim only when the key is pressed. Calling require at spec level would load the plugin while lazy.nvim is still reading the specs, defeating the filetype trigger.',
    choices: [
      {
        value: 'lazy',
        label: 'To keep require out of the spec definition',
        effect: 'The plugin stays lazy until a key is actually pressed.',
      },
      { value: 'error', label: 'Because require would error', effect: 'It would work, but load eagerly.' },
      { value: 'style', label: 'Purely stylistic', effect: 'It has a real effect on load timing.' },
    ],
  }),
];
