import { keyLesson, settingLesson } from './helpers.js';

const topic = 'neotree';

export const neoTreeLessons = [
  keyLesson({
    id: 'neotree.files',
    topic,
    keys: '<leader>fe',
    label: 'Toggle Neo-tree files',
    prompt: 'Toggle the future Neo-tree filesystem explorer on the left.',
    explains: 'This mapping is parked in the real config today, but preserved for when Neo-tree is enabled.',
    siblings: ['<leader>ge', '<leader>fE'],
    sim: 'neotree',
  }),
  keyLesson({
    id: 'neotree.git',
    topic,
    keys: '<leader>ge',
    label: 'Toggle Neo-tree git status',
    prompt: 'Open Neo-tree’s repository-wide git-status explorer.',
    explains: 'This parked mapping groups changed/untracked files in a dedicated Neo-tree source.',
    siblings: ['<leader>fe', '<leader>hq', '<leader>hQ'],
    sim: 'neotree',
  }),
  settingLesson({
    id: 'neotree.hijack',
    topic,
    setting: 'filesystem.hijack_netrw_behavior',
    value: 'disabled',
    label: 'Do not hijack netrw',
    prompt: 'How is Neo-tree configured to coexist with the current netrw setup?',
    explains: 'Directory browsing remains owned by netrw until the migration is intentional.',
    choices: [
      { value: 'disabled', label: 'disabled', effect: ':Explore remains netrw; Neo-tree opens only by command/key.' },
      { value: 'open_default', label: 'open_default', effect: 'Opening a directory replaces netrw with Neo-tree.' },
      { value: 'open_current', label: 'open_current', effect: 'Neo-tree opens in the current window for directory buffers.' },
    ],
    sim: 'neotree',
  }),
  settingLesson({
    id: 'neotree.follow_file',
    topic,
    setting: 'follow_current_file.enabled',
    value: true,
    label: 'Follow current file',
    prompt: 'Should Neo-tree reveal the currently focused file automatically?',
    explains: 'The parked config keeps the tree synchronized with buffer navigation.',
    choices: [
      { value: true, label: 'Follow', effect: 'The tree reveals and selects the active buffer’s file.' },
      { value: false, label: 'Stay put', effect: 'The tree keeps its previous location/selection.' },
    ],
    sim: 'neotree',
  }),
  settingLesson({
    id: 'neotree.watcher',
    topic,
    setting: 'use_libuv_file_watcher',
    value: true,
    label: 'Filesystem watcher',
    prompt: 'Does Neo-tree watch filesystem changes through libuv?',
    explains: 'External file changes appear without manually refreshing the tree.',
    choices: [
      { value: true, label: 'Watch', effect: 'Refreshes the tree when files change outside Neovim.' },
      { value: false, label: 'Manual refresh', effect: 'Reduces watchers but can leave the tree stale.' },
    ],
    sim: 'neotree',
  }),
  settingLesson({
    id: 'neotree.filters',
    topic,
    setting: 'filtered_items',
    value: 'show-all',
    label: 'Show dotfiles and gitignored files',
    prompt: 'Which files remain visible in the parked Neo-tree configuration?',
    explains: 'Both hide_dotfiles and hide_gitignored are false.',
    choices: [
      { value: 'show-all', label: 'Show both', effect: 'Dotfiles and gitignored paths remain visible.' },
      { value: 'hide-dotfiles', label: 'Hide dotfiles', effect: 'Files such as .env and .gitignore disappear.' },
      { value: 'hide-ignored', label: 'Hide gitignored', effect: 'Build/cache paths disappear from the tree.' },
    ],
    sim: 'neotree',
  }),
  settingLesson({
    id: 'neotree.width',
    topic,
    setting: 'window.width',
    value: 34,
    label: 'Neo-tree width',
    prompt: 'How many columns wide is the parked Neo-tree window?',
    explains: 'A fixed 34-column tree is wider than the percentage-based netrw sidebar.',
    choices: [
      { value: 25, label: '25 columns', effect: 'Compact but truncates deep Salesforce paths.' },
      { value: 34, label: '34 columns', effect: 'Current balance for metadata paths.' },
      { value: 50, label: '50 columns', effect: 'More path context but less editing space.' },
    ],
    sim: 'neotree',
  }),
  settingLesson({
    id: 'neotree.close_last',
    topic,
    setting: 'close_if_last_window',
    value: true,
    label: 'Close with last window',
    prompt: 'What happens if Neo-tree is the final remaining window?',
    explains: 'Neovim exits instead of leaving a tree-only editing session.',
    choices: [
      { value: true, label: 'Close Neovim', effect: 'Closing the last editing window exits with Neo-tree.' },
      { value: false, label: 'Keep tree open', effect: 'Leaves a Neo-tree-only window.' },
    ],
    sim: 'neotree',
  }),
  settingLesson({
    id: 'neotree.space_mapping',
    topic,
    setting: 'window.mappings["<space>"]',
    value: 'none',
    label: 'Freeing the leader key',
    prompt: 'Why is <space> mapped to "none" inside the Neo-tree window?',
    explains:
      'Neo-tree binds <space> to toggle_node by default, which would swallow the leader key inside the tree. Setting it to none keeps every <leader> chord usable there.',
    choices: [
      {
        value: 'none',
        label: 'So the leader key still works inside the tree',
        effect: 'Overrides the default toggle_node binding.',
      },
      { value: 'toggle', label: 'It is the default toggle_node', effect: 'That is what is being disabled.' },
      { value: 'unused', label: '<space> is unused by Neo-tree', effect: 'It is bound by default.' },
    ],
    sim: 'neotree',
  }),
  settingLesson({
    id: 'neotree.revival',
    topic,
    setting: 'enabling Neo-tree',
    value: 'keymaps',
    label: 'Turning Neo-tree back on',
    prompt: 'Besides uncommenting the spec, what else must change to enable Neo-tree?',
    explains:
      'The two netrw <leader>f mappings in core/keymaps.lua must go, or they will keep owning <leader>fe. You then choose whether to keep netrw alongside or disable netrwPlugin entirely.',
    choices: [
      {
        value: 'keymaps',
        label: 'Remove the netrw mappings that own <leader>fe',
        effect: 'Otherwise the core keymap wins over the lazy keys entry.',
      },
      { value: 'nothing', label: 'Nothing else', effect: 'The keymap conflict would remain.' },
      { value: 'lockfile', label: 'Delete the lockfile entries', effect: 'They are pinned on purpose.' },
    ],
    sim: 'neotree',
  }),
  settingLesson({
    id: 'neotree.sources',
    topic,
    setting: 'Neo-tree sources',
    value: 'two',
    label: 'Bound and unbound sources',
    prompt: 'How many Neo-tree sources have keymaps in the parked spec?',
    explains:
      'Two: filesystem on <leader>fe and git_status on <leader>ge. The buffers source is available by default but has no binding.',
    choices: [
      {
        value: 'two',
        label: 'Two, leaving the buffers source unbound',
        effect: 'buffers is reachable only via :Neotree buffers.',
      },
      { value: 'three', label: 'All three', effect: 'buffers has no mapping.' },
      { value: 'one', label: 'Only filesystem', effect: 'git_status is mapped too.' },
    ],
    sim: 'neotree',
  }),
  settingLesson({
    id: 'neotree.deviations',
    topic,
    setting: 'Neo-tree defaults',
    value: 'all',
    label: 'Every option is a deviation',
    prompt: 'How many of the parked Neo-tree options match the plugin defaults?',
    explains:
      'None. close_if_last_window, follow_current_file, use_libuv_file_watcher, both filtered_items flags and window.width all differ from their defaults, so the spec is entirely intentional.',
    choices: [
      { value: 'all', label: 'None of them; every option deviates', effect: 'Nothing there is decorative.' },
      { value: 'some', label: 'About half', effect: 'All of them differ.' },
      { value: 'none', label: 'All of them match', effect: 'The opposite is true.' },
    ],
    sim: 'neotree',
  }),
];
